<h1><%= @company.name%></h1>

<%= link_to "Edit company",  edit_company_path(@company)%>
<%= link_to "Delete company", company_path(@company), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  }%>